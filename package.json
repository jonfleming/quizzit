{
  "name": "quizzit",
  "version": "1.0.0",
  "description": "## Project setup ``` npm install ```",
  "main": "server.js",
  "dependencies": {
    "cors": "^2.8.5",
    "csv": "^5.5.0",
    "express": "^4.17.1",
    "express-session": "^1.17.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "postinstall": "cd client && npm i && npm run build && cd .."
  },
  "author": "",
  "license": "ISC"
}
